apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -c
    kompose.version: 1.37.0 (HEAD)
  labels:
    io.kompose.service: piper
  name: piper
spec:
  ports:
    - name: "10200"
      port: 10200
      targetPort: 10200
  selector:
    io.kompose.service: piper
