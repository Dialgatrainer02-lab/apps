apiVersion: v1
kind: Secret
metadata:
  name: openbao-key
  labels:
    app.kubernetes.io/name: {{ include "openbao.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
type: Opaque
data:
  test-1.key: {{ randAlphaNum 32 | b64enc }}