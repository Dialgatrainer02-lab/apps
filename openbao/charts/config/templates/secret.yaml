apiVersion: v1
kind: Secret
metadata:
  name: openbao-key
  labels:
    app.kubernetes.io/name: {{ include "openbao.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
type: Opaque
data:
  key: {{ randalhpanum 32 | b64enc }}